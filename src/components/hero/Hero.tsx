
import React from 'react';
import HeroSection from './HeroSection';

const Hero: React.FC = () => {
  return (
    <section className="relative bg-darkGray">
      <HeroSection />
    </section>
  );
};

export default Hero;
